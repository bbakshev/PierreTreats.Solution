@{
  Layout = "_Layout";
}

@model PierreTreats.Models.Flavor

<h2>Edit @Html.DisplayFor(model => model.Name)</h2>

@using (Html.BeginForm())
{
  @Html.HiddenFor(model => model.FlavorId)

  <p>@Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger", @style = "font-weight: 600; color:red;" })</p>
  @Html.Label("Flavor: ")
  @Html.EditorFor(model => model.FlavorId)
  <br><br>
  <input type="submit" value="Save"/>
}

  <span>@Html.ActionLink("Back to list", "Index")</span>